# Handle line endings for all text files
* text=auto

# Force specific line endings for certain types of files
*.sh   eol=lf     # Shell scripts always use LF
*.bat  eol=crlf   # Batch scripts always use CRLF
*.md   text       # Markdown files as text (handled automatically)

# Prevent Git from messing with binary files
*.jpg  -text     
*.png  -text
